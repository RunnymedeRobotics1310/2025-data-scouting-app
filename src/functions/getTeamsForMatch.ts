export function getTeamsForMatch(
  tournamentId: string,
  matchNumber: number,
): number[] {
  //TODO: implement this
  console.log('Finding teams for match ' + matchNumber + ' at ' + tournamentId);

  return [1310, 854, 9262, 2056, 1325, 865];
}
